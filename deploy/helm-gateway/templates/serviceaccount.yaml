{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubetty-gateway.serviceAccountName" . }}
  labels:
    {{- include "kubetty-gateway.labels" . | nindent 4 }}
{{- end }}
