{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kubetty-project.serviceAccountName" . }}
  labels:
    {{- include "kubetty-project.labels" . | nindent 4 }}
{{- end }}
